version:
  file: VERSION

changelog:
  files:
    - path: CHANGELOG.md
      lang: zh

commit:
  message: "chore: release v{version}"

tag:
  prefix: v
  sign: false

include:
  - VERSION
  - CHANGELOG.md