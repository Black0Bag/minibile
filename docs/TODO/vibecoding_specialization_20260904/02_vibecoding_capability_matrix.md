# 02 VibeCoding 能力矩阵

## 旧实现情况

当前功能按通用 Agent 逐年叠加。手机控制、Android 调试、浏览器自动化、截图、Shizuku 等底层能力存在用途重叠，不能凭名称整块删除。

## 意图修正

以“是否直接支撑编码任务闭环”为唯一标准，对所有用户入口和底层能力做可审计分类。

## 预期新实现

建立矩阵覆盖：

- 全部内置工具
- ToolPkg/MCP/Skill 能力
- 所有导航路由和设置页
- Manifest Activity/Service/Receiver/Provider 与权限
- Gradle 模块和生产依赖
- assets、native 库、项目模板和多语言资源
- Provider 和功能模型类型

每项必须标记：`保留`、`拆分后保留`、`删除`，并附入口、反向依赖、VibeCoding 场景和验证方法。

## 作用域

只读源码分析和文档。不得在矩阵确认前删除业务文件。

## 验证

矩阵计数与自动扫描一致；所有删除项都有反向依赖结果；所有保留项都有至少一个具体编码场景；用户完成产品取舍确认。

## 风险

- 将 Android 开发调试误判为手机控制
- 将底层共享依赖随上层产品功能一起删除
- 漏掉动态注册工具或 Manifest 入口

## 回滚

矩阵是文档资产。发现遗漏时更新矩阵并重新走用户确认，不因先前分类自动触发代码删改。

## 实测扫描结果

已完成只读扫描，矩阵见 [`02_capability_matrix.md`](02_capability_matrix.md)。计数与自动扫描一致：内置工具 179、扩展包 31、Screen 68、Manifest 组件 34、权限 37、Gradle 模块 9、Provider 21、功能模型 10、语言 9、模板 9、依赖 192。

## 用户确认状态（2026-09-09 全部定稿）

工具级 179 项（保留 101 / 删除 78）与全部 7 个取舍点（2 工具级 + 5 模块/权限/资产级）均已获用户确认，判定变更记录见矩阵第十节、十四节。

## [DONE] T02 定稿（2026-09-09）

- 179 工具全量清单落盘（`02_capability_matrix.md` 第十一~十四节），脚本核算明细 179 行、双向零差集
- 用户确认改判：浏览器自动化 24 全留、`update_user_profile` 保留、`capture_screenshot`/`install_app`/`uninstall_app` 保留、工作流 10 维持删除
- 搜索扩展包 4 个全保留，`various_search`/`duckduckgo` 默认停用
- `QUERY_ALL_PACKAGES` 删除、悬浮窗与子包签名保留、本地推理模块删除、`assets/shizuku.apk` 修正为保留
- 验证标准全部满足：计数与自动扫描一致、删除项有反向依赖结果（源码证据列）、保留项有编码场景（依据列）、用户完成取舍确认
